1. Create venv (click on the python version at the bottom right of vscode)

2. Install the dependencies (pip install -r requirements.txt)

3. To run the fast API you can either do fastapi run main.py OR uvicorn main:app --reload
You can open http://127.0.0.1:8000/docs to see the docs